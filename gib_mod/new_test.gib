use std::String
use std::Int

trait ToString {
  fn string(): String
}

impl ToString for Int {
  fn string(): String {
  }
}

struct Test {
  name: String,
}

fn test() {
  let Test { name: some_name } = Test("Hello")
}
