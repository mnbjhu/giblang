use std::println
use std::Int
use std::Int

fn print_int(int: Int) {}

trait Display {
    fn Self.to_string(): std::String {
        "Display"
    }
}

trait Unwrap[T] {
    fn Self.unwrap(): T {}
}

impl[U] Unwrap[U] for std::Option[U] {
    fn Self.unwrap(): U {}
}

fn main() {}

enum MyEnum   {
    First,
    Second(Int),
    Third {
        test: Int,
        test2: Int
    }
}
