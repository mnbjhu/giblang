use std::println
use std::Int
use std::Int

fn print_int(int: Int) {}

trait Display {
    fn Self.to_string(): std::String {
        "Display"
    }
}

trait Unwrap[T] {
    fn Self.unwrap(): T {}
}

impl[T] Unwrap[T] for std::Option[T] {
    fn Self.unwrap(): T {}
}

fn main() {}

enum MyEnum   {
    First,
    Second(Int),
    Third {
        test: Int,
        test2: Int
    }
}
