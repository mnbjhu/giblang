use std::println
use std::Int
use std::Int

fn print_int(int: Int) {}

trait Display {
    fn Self.to_string(): std::String {
        "Display"
    }
}

fn main() {}

enum MyEnum   {
    First,
    Second(Int),
    Third {
        test: Int,
        test2: Int
    }
}
