use gib_mod::another::Test3

trait ToStr {
  fn Self.to_str(): String
}

impl ToStr for Thing {
  fn Self.to_str(): String {
    "Thing String"
  }
}

struct Thing {
  test: Int,
  even_more: String
}

struct Other {
  inner: Thing
}

fn main() {
  let test = "Hello, World!"
  print(test)
  another()
}

fn another(): ToStr {
  let test: ToStr = Thing(123, "Hello")
  print(test.to_str())
  test
}

enum TestEnum {
  A,
  B(Int),
  C {
    some: Int,
    other: String
  }
}
