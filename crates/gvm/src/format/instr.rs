use super::literal::Literal;

#[derive(Debug)]
pub enum ByteCode {
    Push(Literal),
    Copy,
    Pop,
    Print,
    Panic,
    Construct { id: u32, len: u32 },
    Dyn(u64),
    Call(u32),
    DynCall(u32),
    Return,
    Index(u32),
    SetIndex(u32),

    VecGet,
    VecSet,
    VecPush,
    VecPop,
    VecPeak,
    VecInsert,
    VecRemove,
    VecLen,

    NewLocal(u32),
    GetLocal(u32),
    SetLocal(u32),
    Param(u32),
    Goto(u32),
    Je(i32),
    Jne(i32),
    Jmp(i32),
    Add,
    Mul,
    Sub,
    Or,
    And,
    Not,
    Eq,
    Neq,
    Lt,
    Gt,
    Lte,
    Gte,
    Match(u32),
    Clone,
}
