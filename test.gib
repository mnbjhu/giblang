fn simple[T](thing: T): Option[T] {
}

enum Option[T] {
  Some(T),
  None
}

enum Result[R, E] {
  Ok(R),
  Err(E)
}

struct Box[T](T)

fn main() {
  let first: Option[()] = Option::None
  let test = match first {
    Option::Some(some) => Result::Ok("Hello"),
    Option::None => Result::Err(123)
  }
}
