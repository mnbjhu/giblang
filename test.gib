fn simple[T](thing: T): Option[T] {
}

enum Option[T] {
  Some(T),
  None
}

enum Result[R, E] {
  Ok(R),
  Err(E)
}

struct Box[T](T)

fn test(thing: String) {}

fn main() {
  let test2: Option[String] = Option::Some(123)
  test(123)
}
