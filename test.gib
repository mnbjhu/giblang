use std::String
use std::Int
use std::println
use std::print
use std::Option
use std::Result

trait Test {
    fn another()
}

struct Box[T](T)

fn test(thing: Test) {
    let other = 123
}

struct Basic {
    test: Int,
    test2: String
}

fn thingy(x: Option[String]) {}

fn test3(thing: Int) {
    let some = Option::Some(thing)
    some.unwrap()
}

fn test123() {
    let other = Option::Some("")
    Basic(123, other.unwrap())
}
